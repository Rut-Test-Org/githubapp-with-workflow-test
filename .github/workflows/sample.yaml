name: Sample Workflow

on:
  workflow_run:
    workflows: [Avoma Frontend Version Bump]
    types:
      - completed
    branches:
      - main

jobs:
  version-bump:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello World"
